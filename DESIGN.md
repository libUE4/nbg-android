# NBG Android 当前设计文档

> 记录当前项目的 UI / UX 设计基线，基于 2026-06-29 的代码状态。这个文档描述“现在是什么样”，不是下一版改造方案。

## 1. 产品定位

NBG Android 是一个移动端 AI 编程 / Agent 对话工具。主界面不是普通聊天软件，而是把 AI 回复、终端、文件、Diff、MCP、Skills、模型配置和运行状态整合在同一个移动工作台里。

核心场景：

- 在手机上和 HanakoPro / NBG-Code 进行连续对话。
- 查看 AI 的流式回复、思考状态、工具调用状态和终端输出。
- 管理 URL API 模型、MCP 服务、Skills、桌宠、终端和文件共享。
- 在小屏幕上保留足够高的信息密度，同时保持可触控、可扫描。

## 2. 设计原则

- 工具优先：视觉装饰让位于对话、命令输出、代码和状态信息。
- 高密度但不拥挤：消息间距和工具卡片保持紧凑，避免大面积空白。
- 状态可扫描：运行中、成功、失败、禁用、选中必须通过颜色、文字和图标同时表达。
- 移动端优先：按钮触控区域不低于 32-44dp，输入栏跟随键盘和导航栏。
- 主题统一：所有页面使用 `NbgAgentColors`，不在组件里硬编码独立色系。
- 动效克制：只保留进入、展开、按压、状态呼吸等短动效，避免影响流式输出性能。

## 3. 信息架构

主导航由会话抽屉和子页面组成：

- 聊天页：默认主界面，承载消息流、顶部栏、底部输入栏和桌宠浮层。
- 会话抽屉：会话搜索、会话列表、快捷入口。
- 终端：独立终端页，使用终端自己的状态栏和返回按钮。
- FTP 共享：文件共享入口。
- URL API：管理模型接口、分组、模型列表和上下文信息。
- MCP 服务：管理连接器、启动 / 停止、刷新工具、agent/tool 启用状态。
- Skills：管理技能、bundles、external paths、reload、翻译和安装。
- 桌宠：管理已安装桌宠、当前桌宠、动作库和 PetDex 安装入口。
- 外观：主题和字体选择。

主要源码位置：

- 主题：`app/src/main/java/com/nbg/android/NbgAgentTheme.kt`
- 聊天页：`app/src/main/java/com/nbg/android/NbgAgentChatUi.kt`
- 输入栏：`app/src/main/java/com/nbg/android/NbgAgentComposerUi.kt`
- 消息和工具卡片：`app/src/main/java/com/nbg/android/NbgAgentMessageUi.kt`
- 抽屉：`app/src/main/java/com/nbg/android/NbgAgentDrawerUi.kt`
- MCP：`app/src/main/java/com/nbg/android/NbgAgentMcpUi.kt`
- Skills：`app/src/main/java/com/nbg/android/NbgAgentSkillsUi.kt`
- 桌宠：`app/src/main/java/com/nbg/android/NbgPetsUi.kt`、`NbgPixelPetUi.kt`

## 4. 主题系统

当前保留 3 个主题预设：

| ID | 名称 | 方向 | 主强调 |
| --- | --- | --- | --- |
| `light` | 默认白色 | 干净白色工作台，适合白天阅读 | 蓝色 `#2F5F9D` |
| `black` | 黑色 | 接近 Codex / Claude Code 的黑色工具界面 | 近白 `#E8EAED` |
| `claude` | Claude 暖色 | 暖灰、低饱和、琥珀强调 | 琥珀 `#E9C46A` |

### 默认白色主题

- 背景：`#F7F7F4`
- 顶栏：`#FDFDFB`
- 抽屉 / 卡片：`#FFFFFF`
- 输入栏：`#FFFFFF`
- 输入边框：`#D7D4CA`
- 用户消息：`#E9EDF5`
- 助手消息：`#FFFFFF`
- 代码块 / 终端内容：黑底，保证代码阅读对比度。

### 黑色主题

- 背景：`#090A0B`
- 顶栏：`#0D0E10`
- 抽屉：`#111214`
- 卡片：`#191B1F`
- 输入栏：`#111317`
- 用户消息：`#262C35`
- 助手消息：`#111317`
- 文本：`#F3F4F6`

### Claude 暖色主题

- 背景：暖黑 / 暖灰。
- 输入栏和卡片使用低饱和棕灰。
- 主强调色使用琥珀，避免整页变成高饱和橙色。

## 5. 字体系统

字体在外观页中可选：

| ID | 名称 | 用途 |
| --- | --- | --- |
| `system` | 系统 | Android 默认字体，兼容性最好 |
| `ios` | iOS | 接近 San Francisco 的无衬线观感 |
| `sans` | 清爽 | 现代 SaaS 风格 |
| `serif` | 书卷 | 长文本更有层次 |
| `mono` | 代码 | 更像开发工具，数字和代码对齐更稳定 |

正文默认 14-15sp，辅助文字 10-12sp，标题 15-18sp。聊天页避免使用大标题，保持工具界面的密度。

## 6. 聊天页骨架

聊天页使用 `Scaffold`：

- `topBar`：`NbgChatTopBar`
- `content`：`LazyColumn` 消息列表
- `bottomBar`：运行状态、Todo、确认卡片、`NbgChatComposer`
- 浮层：滚动到底部按钮、桌宠浮层

消息列表当前布局：

- 左右 padding：`16.dp`
- 顶部 padding：顶部栏高度 + `10.dp`
- 底部 padding：使用 `innerPadding.calculateBottomPadding()`，不再额外硬塞大 padding。
- 消息间距：`10.dp`
- 底部锚点：`2.dp` spacer，用于恢复和滚动到底部。

滚动逻辑：

- 用户在底部时，AI 输出会跟随到底部。
- 用户手动离开底部时，`followOutput` 关闭，避免强行抢滚动。
- 非底部时显示 44dp 的“移动到底部”按钮。
- 恢复会话时通过会话路径和消息签名生成滚动 key，优先恢复到底部。

## 7. 顶部栏

组件：`NbgChatTopBar`

- 高度：`68.dp`
- 背景：`TopBar`
- 水平 padding：`10.dp`
- 左侧：菜单按钮。
- 中间：会话标题 + 运行状态行。
- 右侧：更多菜单 + 新建会话。

更多菜单包含：

- 压缩上下文：仅在可压缩、未流式输出、未压缩中时可用。
- 重新生成：重放最新 turn。
- 撤回上一轮：回退最新 turn。

状态行通过小圆点和文字表达连接、运行、错误等状态，运行中状态允许轻微呼吸动画。

## 8. 底部输入栏

组件：`NbgChatComposer`

当前输入栏是悬浮感卡片：

- 外层使用 `imePadding()` 和 `navigationBarsPadding()`，适配键盘和系统导航。
- 外边距：水平 `10.dp`，垂直 `8.dp`。
- 主卡片圆角：`27.dp`
- 主卡片底部留出 `34.dp`，露出下面的半层工具 dock。
- 阴影：两层 shadow，形成轻量悬浮感。
- 背景：`Input`，透明度 `0.985f`
- 边框：`InputBorder`，透明度 `0.68f`

文本输入区：

- 占位：`输入消息`
- 字号：`15.sp`
- 行高：`21.sp`
- 高度：最小 `32.dp`，最大 `82.dp`
- 光标颜色：`Primary`

输入操作区：

- 左侧 `+`：添加上下文，目前提示“稍后接入”。
- `思考`：打开思考强度弹层。
- `权限`：打开权限模式弹层。
- `模型`：打开模型选择弹层。
- 发送按钮：44dp 圆形按钮，支持发送、插话、停止三种状态。

底部半层 dock：

- 高度：`56.dp`
- 形状：底部圆角 `24.dp`
- 露出在主输入卡片下方，像被主卡片压住的第二层面板。
- 当前按钮：`连接 IM`、`Agents team`，功能仍是占位提示。

## 9. 思考强度弹层

思考按钮不直接显示 `off / auto / low / medium / high / xhigh` 字符串，而是用中文标签和滑动强度表达。

当前逻辑：

- 根据当前模型能力计算可选思考等级。
- 如果模型不支持最大思考，弹层不应强行展示不可用状态。
- Claude 系列默认使用官方保守上下文和思考能力判断，不盲目按反代返回值扩大。
- 弹层带短动画、进度条和流动高光，用于表达思考强度。

注意：`<thinking>...</thinking>`、`<think>...</think>` 等标签应该被解析到思考消息，不应作为普通回复正文直接显示。

## 10. 消息设计

消息类型：

- 用户消息：右侧气泡。
- 助手消息：左侧正文，支持 Markdown。
- 思考消息：独立思考卡片 / 状态块。
- 工具消息：工具状态卡片。
- 内容块：文件、Diff、终端等预览内容。
- 系统消息：低优先级状态说明。

用户消息：

- 右对齐。
- 背景使用 `UserBubble`。
- 文字 15sp 左右。
- 圆角小于输入栏，避免抢主视觉。

助手消息：

- 左对齐。
- 正文优先 Markdown 渲染。
- 历史消息和流式消息都应保持统一排版，避免结束后突然变成另一种视觉结构。
- Markdown 中的表格、代码块、引用、列表要按主题适配，不在白色主题下出现不协调的大黑块，除非是代码 / 终端内容。

消息进入动效：

- 新消息使用 150-180ms 的淡入和轻微上移。
- 历史恢复不应该触发大量动效，避免卡顿。

## 11. Markdown 与代码块

Markdown 渲染目标：

- 普通文本保持轻量，不额外套廉价大卡片。
- 代码块使用终端 / 编辑器感的深色块，保证对比度。
- 表格要压缩行高，避免 `|------|` 这类原始 Markdown 在最终回复中裸露。
- 流式输出期间尽量增量渲染，减少“输出结束才突然变成卡片”的跳变。

当前设计要求：

- 普通 AI 回复不是卡片。
- Markdown 结构才使用局部样式。
- 代码和终端输出允许黑底。
- 白色主题下的非代码正文块不能过黑。

## 12. 工具卡片

组件：`NbgToolStatusCard`

普通工具卡片：

- 宽度填满消息区域。
- 圆角：`14.dp`
- 背景：`SurfaceContainer`，Diff 时透明度更低。
- 边框：运行中使用 `PrimarySoft`，非运行使用 `LiquidBorder`。
- 左侧图标块：30dp，圆角 10dp。
- 标题：13sp 半粗体。
- 副标题：11sp，带 6dp 状态点。
- 详情文字：11sp，最多 2 行。

交互：

- 可展开行内预览。
- 可打开详情弹窗。
- 展开 / 收起使用 120-180ms 的高度和透明度动画。

## 13. 终端与 Diff 卡片

终端类工具使用 `NbgCompactTerminalToolCard`：

- 圆角：`14.dp`
- 背景：`SurfaceContainer`，透明度约 `0.72f`
- 内边距：水平 `8.dp`，垂直 `6.dp`
- 行高紧凑，标题 12sp，摘要 10sp。
- 右侧提供“输出 / 收起”和打断按钮。

终端预览框使用黑色仿终端风格：

- 顶部保留类似 macOS 终端的三个圆点。
- 三个圆点在左上角。
- 内容区域使用等宽字体。
- 历史终端输出不能丢失，静态输出和实时输出要统一进入预览。

Diff 预览：

- 增加行使用 `DiffAddSurface`。
- 删除行使用 `DiffDeleteSurface`。
- 不再使用廉价大黑框包住所有 Diff。
- 只在代码 / diff 内容本身使用等宽布局。

## 14. 会话抽屉

组件：`NbgConversationDrawer`

抽屉承担两个任务：

- 快速进入功能页。
- 搜索和切换会话。

搜索框：

- 默认显示，不需要先点搜索按钮。
- 圆角：`16.dp`
- 背景：`SurfaceContainer`
- 边框：`LiquidBorder`
- 图标：搜索图标。
- 占位：`搜索 HanakoPro 会话`
- 清空按钮：30dp 小圆角按钮。

会话行：

- 选中态使用 `Selected`。
- 支持点击切换。
- 支持长按菜单：重命名、置顶 / 取消置顶、删除。
- 行内文本必须单行省略，避免挤压操作区域。

快捷入口：

- 新建对话
- 终端
- FTP 共享
- 网址 API
- MCP
- Skills
- 桌宠
- 外观

## 15. URL API 页面

URL API 页面用于管理外部模型接口。

设计目标：

- 分组、模型、上下文、启用状态都要可扫描。
- 模型选择处不应重复显示冗余网址前缀。
- Claude 上下文默认按官方 200k 保守展示，除非后续有可靠上游能力字段。
- 分组删除后，聊天页模型分组和缓存应同步刷新。

按钮使用 `NbgInlineActionButton`：

- 高度：`36.dp`
- 圆角：`13.dp`
- 图标块：22dp
- 标签：12sp
- 选中态使用 `PrimaryContainer`

## 16. MCP 页面

组件：`NbgMcpScreen`

当前功能结构：

- 总开关：启用 / 禁用 MCP。
- 摘要卡：连接器数量、运行中数量、当前 agent。
- 添加连接器：名称、命令、环境变量、transport。
- 连接器行：状态、启动 / 停止、刷新工具、删除。
- agent 级启用：每个连接器可按 agent 启用。
- tool 级启用：每个工具可单独启用 / 禁用。

视觉：

- 摘要卡圆角 18dp。
- 连接器卡片和工具行使用统一 Surface / Border。
- 删除是显式危险操作，需要确认弹窗。

## 17. Skills 页面

组件：`NbgSkillsScreen`

当前功能结构：

- 摘要卡：技能数量、加载状态、reload。
- bundles：创建、编辑、删除技能组合。
- external paths：管理外部技能路径。
- 技能列表：启用 / 禁用、详情、删除。
- 安装：支持新增技能输入。
- 翻译：不是直接翻译所有 skills，而是在单个技能卡片内切换显示中文说明。

视觉：

- 技能卡片保持紧凑。
- 翻译状态不应该污染思考内容区域。
- 长描述需要折叠或限制高度，避免一张卡片占满屏幕。

## 18. 桌宠

桌宠当前作为聊天页浮层，不进入消息文本流。

聊天页浮层：

- 默认停靠在右侧，完整显示。
- 位置由用户拖动决定。
- 不自动在消息文本里走动，避免遮挡正文。
- 输入框激活时降低透明度，减少遮挡。
- 点击桌宠打开菜单。
- 点击空白区域关闭菜单。

桌宠页面：

- 展示当前桌宠。
- 展示动作库。
- 展示已安装列表。
- 可隐藏 / 显示。
- 可删除已安装桌宠。
- 可从 PetDex manifest 搜索和安装。

动作原则：

- 空闲时低频动作。
- 思考、输出、工具调用、错误状态使用对应动作。
- 流式输出时动作不能过于频繁，避免拖慢聊天页面。

## 19. 终端页面

终端页是独立工具页面，不使用聊天页顶部 app 状态栏。

设计要求：

- 保留终端自身状态栏。
- 页面顶部只需要返回按钮和必要标题。
- 终端内容使用等宽字体。
- 历史输出不能因为页面切换丢失。
- 终端卡片和终端页面的视觉语言要统一，但页面终端不需要被包成聊天卡片。

## 20. 动效规范

当前动效集中在：

- 按压：`NbgPressBox`，按下缩放到约 `0.96f`，透明度降低。
- 消息进入：150-180ms 淡入 / 位移。
- 卡片展开：120-180ms fade + expand。
- 滚动到底部按钮：淡入 + 轻微上移。
- 输入 chip：160ms 颜色 / 图标缩放。
- 思考强度弹层：弹出、进度、流动高光。
- 桌宠：低频脉冲、状态动作。

动效限制：

- 不使用持续高成本背景动画。
- 流式输出时避免大量重组触发复杂动画。
- 遵守系统动画开关：`nbgMotionDuration()` 会在系统关闭动画时返回 0。

## 21. 组件规范

通用图标按钮：`NbgPlainIconButton`

- 大小：40dp
- 圆角：13dp
- 背景：`SurfaceContainer`
- 图标：19dp
- 使用 `NbgPressBox` 提供按压反馈。

小按钮：`NbgMiniIconButton`

- 大小：28dp
- 圆角：11dp
- 用于工具卡片内部操作。

行内操作按钮：`NbgInlineActionButton`

- 高度：36dp
- 适合 URL API / MCP / Skills 这类设置页。

搜索框：

- 抽屉和桌宠搜索保持当前原生样式。
- 不使用 hover 才展开的 Web 风格，因为移动端没有 hover。

## 22. 无障碍与可用性

- 关键按钮需要 `contentDescription`。
- 仅靠颜色表达状态是不够的，状态文字必须保留。
- 长文本必须 `maxLines + ellipsis`，避免挤压按钮。
- 文本不能使用 viewport 相关缩放。
- 小屏幕下输入栏按钮不能换行到不可触控状态。
- 弹窗必须支持点击外部或返回关闭，危险操作除外。

## 23. 当前已知设计债务

- `NbgAgentChatUi.kt`、`NbgAgentComposerUi.kt`、`NbgAgentMessageUi.kt` 仍然偏重，后续可以继续拆分。
- 输入栏底部 dock 的 `连接 IM` 和 `Agents team` 仍是占位功能。
- 添加上下文按钮仍是占位提示。
- Markdown 流式渲染仍需要继续优化，避免结束后视觉跳变。
- 工具卡片、终端卡片、Diff 卡片的密度已压缩，但还可以继续统一成更清晰的组件层级。
- 桌宠动效需要继续控制重组和刷新频率，避免 AI 输出时卡顿。
- MCP / Skills 功能已补齐主入口，但细节交互还需要继续跟 PC 端对齐。

## 24. 不再采用的实验

以下内容已经回退，不属于当前设计基线：

- Uiverse hover 展开删除按钮。
- 搜索框右侧固定 `⌘K` 风格按钮。
- 大面积 Liquid Glass / 彩虹渐变背景。
- 聊天页办公室场景。
- 桌宠在消息文本里自由走动。
- 输入框底部呼吸灯。

后续如果重新引入这些方向，需要先重新评估移动端触控、性能和当前主题一致性。
